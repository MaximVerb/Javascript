const isRaining = true;
//isRaining = false;
console.log(isRaining);

const person = {
name : 'Maxim',
age: 28, }

person.name = 'Max'; // je kunt eigenschappen wel wijzigen !!
// person = {} --> kan niet, het kan dus geen leeg object zijn

console.log(person);